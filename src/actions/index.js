export const changeTextareaValue = (val) => ({
	type: 'TRANSFORM_VAL',
	val
});

export const transformToHtmlTags = (html) => ({
	type: 'TRANSFORM_TAGS',
	html
});

export const addFont = (link) => ({
	type: 'ADD_FONT',
	link
})

